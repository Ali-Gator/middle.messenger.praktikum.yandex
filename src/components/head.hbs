<!doctype html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, user-scalable=yes, initial-scale=1.0">
  <title>Tne Messenger</title>
  <link rel="stylesheet" href="{{resolve-from-root 'style.css'}}" />
  <link rel="stylesheet" href="{{resolve-from-root '../static/fonts.css'}}" />
  {{#if cssFile}}
    <link rel="stylesheet" href="{{resolve-from-root cssFile}}" />
  {{/if}}
</head>
<body>
{{> @partial-block }}
</body>
</html>
